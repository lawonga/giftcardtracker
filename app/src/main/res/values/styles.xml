<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!--<item name="drawerArrowStyle">@style/Hamburger</item>
        <item name="homeAsUpIndicator">@drawable/ic_drawer</item>-->
        <!--<item name="android:editTextStyle">@style/App_EditTextStyle</item>-->
    </style>

    <style name="Hamburger" parent="@style/Widget.AppCompat.DrawerArrowToggle">
        <item name="spinBars">true</item>
        <item name="color">@android:color/white</item>
        <item name="android:drawable">@drawable/ic_drawer</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!--<style name="App_EditTextStyle">
        <item name="android:background">#DDDEEE</item>
        <item name="android:textColor">#808080</item>
        <item name="android:textSize">24dp</item>
    </style>-->

    <style name="AndroidImageSlider_Pure_White">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:gravity">center</item>
        <item name="android:paddingLeft">2dp</item>
        <item name="android:paddingRight">2dp</item>
        <item name="shape">oval</item>
        <item name="selected_color">#FFFFFF</item>
        <item name="unselected_color">#FFFFFF</item>
        <item name="selected_width">8dp</item>
        <item name="selected_height">8dp</item>
        <item name="unselected_width">8dp</item>
        <item name="unselected_height">8dp</item>
    </style>

</resources>
